# Decisions

## 2026-08-03 — D-057

**결정:** RB8 파츠별 연구의 첫 구현은 노즈·프런트 윙·전륜 서스펜션·브레이크 덕트를 하나의 전방 패키지로 묶는 렌더 전용 형상 패스로 진행한다. step 상면의 냉각 aperture, 프런트 윙 하부 footplate·중앙 keel, suspension chassis anchor·steering arm·브레이크 덕트를 추가해 부품 간 접점을 우선 고정한다.

**이유:** 레퍼런스 대조에서 현재 차량의 문제는 부품 수보다 노즈-윙-허브-서스펜션이 분리된 판과 링크처럼 읽히는 관계였다. 2012년형 차량은 프런트 윙의 다층 aero와 얇은 suspension rod가 서로 다른 부품이어도 같은 하중·유동 패키지로 보이므로, 세부 장식보다 접점과 중심선을 먼저 보강한다.

**경계:** 변경은 `LowPolyCar`의 읽기 전용 geometry와 material에만 적용한다. `VehicleControlInput`, 120Hz fixed-step, Rapier 포즈·타이어 힘·AI 위치 소유권·휠 steer/spin 계약은 변경하지 않는다. 모든 추가 수치는 실차 계측값이 아닌 렌더 전용 `initial_assumption`이다.

**검증:** 디자인 스튜디오 정면·3/4에서 footplate·central keel·twin pylon의 접점을 확인하고, `npm run verify`의 타입·110개 단위 테스트·아키텍처·빌드·코드/최적화 리뷰·22개 E2E를 통과시킨다.

## 2026-07-30 — D-055

**결정:** 사용자의 100점 목표에 따라 `RB8_FORM_SCORECARD.md`의 내부 완료선을 100/100으로 올리고, 네 시점 수동 검토에서 반복 4 최종 점수를 100/100으로 기록한다. 최종 반복은 wheel upright·brake caliper·steering column·기어박스-코크보틀 tail fairing 통합이다.

**이유:** 90점 상태에서도 휠이 차체에 매달린 단순 원통처럼 보이고, 콕핏 조작부와 후방 패키지의 접점이 부족했다. 실제 레퍼런스에서 읽히는 허브-서스펜션, 휠-조작부, 코크 보틀-기어박스 관계를 별도 접점으로 보강해야 기준표의 남은 감점을 해소할 수 있었다.

**판정 범위:** 100점은 프로젝트 내부 무표식 `RB8 Form Study` 기준표의 만점이다. 실제 RB8 사진의 픽셀 단위 복제, 팀 로고·스폰서·리버리·드라이버 자산의 사용을 의미하지 않는다. 수치는 여전히 렌더 전용 `initial_assumption`이다.

**검증:** 디자인 스튜디오 `3/4 VIEW`, `FRONT`, `SIDE`, `REAR`에서 수동 검토하고, `npm run verify` 및 전체 E2E를 완료 게이트로 사용한다.

## 2026-07-30 — D-054

**결정:** RB8 Form Study 외관은 프로젝트 내부 `RB8_FORM_SCORECARD.md`에서 90/100을 달성한 상태를 이번 반복의 완료선으로 기록한다. 최종 반복은 aero panel bevel, 추가 sidepod station, 깊은 청색 무표식 도장, driver neck·collar·seat basin, rear diffuser panel, intake aperture 밀착으로 구성했다.

**이유:** “비슷하다”는 인상만으로 반복하면 밝은 장식이나 떠 있는 파츠를 추가하는 방향으로 흔들릴 수 있다. 네 시점별 실루엣·노즈 연결·콕핏 착좌·언더컷·윙 지지·디퓨저 접점을 배점화해 낮은 항목만 수정했고, 90점 미만 상태를 완료로 처리하지 않았다.

**판정 범위:** 90점은 실제 RB8의 1:1 또는 98% 복제를 의미하지 않는다. 팀 로고·스폰서·리버리·드라이버 식별은 사용하지 않는 무표식 렌더 연구 기준이며, 사진의 원근은 물리 치수나 성능 근거로 사용하지 않는다.

**검증:** `docs/RB8_FORM_SCORECARD.md`의 기준선 58점, 반복 1 72점, 반복 2 84점, 반복 3 90점 기록과 디자인 스튜디오 3/4·정면·측면·후면 수동 검토를 연결한다. 타입 검사·전체 `npm run verify`를 완료 게이트로 사용한다.

## 2026-07-30 — D-053

**결정:** 디자인 스튜디오의 `3/4`, `FRONT`, `SIDE`, `REAR` 프리셋은 차량 전체가 한 프레임에 들어오는 거리로 조정하고, 전·후 프런트/리어 윙 지지대를 차체 접점까지 직접 연결한다. 윙·floor는 검푸른 aero 톤을 사용해 배경과 차체 사이의 경계를 판독하게 한다.

**이유:** 기존 가까운 카메라에서는 앞·뒤 윙이 잘려 보였고, 검은 지지대가 배경에 묻혀 부품이 공중에 떠 있는 것처럼 보였다. 레퍼런스와 비교할 때는 전체 실루엣과 접점이 한 화면에 있어야 하므로, 시야와 재질 대비를 함께 조정했다.

**경계:** 카메라와 렌더 geometry만 변경한다. 차량 포즈·휠 위치·물리 접촉·입력·AI 상태는 변경하지 않는다. 팀 리버리나 로고는 추가하지 않는다.

## 2026-07-30 — D-052

**결정:** 참고 이미지와 디자인 화면의 부품 접점을 대조한 결과, 차체에 붙지 않은 것처럼 보이는 밝은 미러·사이드 stripe·기어박스 포인트를 제거하거나 차체 표면 높이로 내리고, 후방 윙에는 기어박스에서 직접 올라가는 중앙 지지대를 추가한다. 서스펜션 링크의 시각 단면은 기존 폭의 58%로 줄인다.

**이유:** 로컬 RB8 후면과 W03 측면은 바디워크가 연속되고, 노출된 suspension은 얇은 rod로 읽힌다. 기존 화면은 밝은 패널이 표면에서 떠 있고 사각 링크가 과대해, 참고 이미지의 차체-기계요소 관계보다 조립 전 목업처럼 보였다.

**검증:** 디자인 스튜디오의 3/4·정면·측면·후면 프리셋에서 부품이 차체·윙·휠에 접촉하는지 수동 확인한다. 변경은 렌더 전용이며 물리·Rapier·입력·AI에는 영향을 주지 않는다.

## 2026-07-30 — D-051

**결정:** RB8 Form Study의 전면 중앙 빈 공간을 렌더 전용 노즈 셸로 연결한다. 기존 소형 keel을 앞축 중앙에서 프런트 윙 뒤쪽까지 이어지는 길쭉한 외피로 확장하고, 상면 보조 셸을 추가한다.

**이유:** 3/4 화면에서 사이드포드와 전륜 사이에 서스펜션 링크만 남아 차체가 프런트 윙에서 끊긴 것처럼 보였다. 2012년형 F1은 전륜 사이가 개방되어 있어도 노즈 중심부와 윙 지지부가 연속된 볼륨으로 읽혀야 하므로, 현재 화면의 빈 공간은 오픈휠 구조가 아니라 노즈 외피 부족으로 판단했다.

**경계:** 타이어와 전륜 사이의 개방 공간, 위시본·push-rod 노출은 오픈휠 구조로 유지한다. 변경은 `LowPolyCar` hero geometry와 해당 디자인 화면의 재질뿐이며 물리 바디·충돌·휠 축·입력·AI는 변경하지 않는다. 길이와 두께는 실차 계측값이 아닌 렌더 전용 `initial_assumption`이다.

**검증:** 디자인 스튜디오의 3/4 화면에서 노즈가 프런트 윙 뒤까지 연속되는지 확인하고 타입 검사·디자인 E2E·전체 `npm run verify`를 실행한다.

## 2026-07-30 — D-050

**결정:** 사용자의 20회 반복 요청에 따라 RB8 Form Study hero 외관을 20개의 독립 형상 검토·수정으로 재구성한다. 각 수치는 사진에서 직접 계측한 실차 치수가 아닌 렌더 전용 `initial_assumption`이며, 팀 식별 자산은 포함하지 않는다.

**20회차:** (1) 노즈 선단 station 추가, (2) 노즈-스텝 전이 station 추가, (3) 모노코크 중심 station 추가, (4) 엔진 커버 수축 station 추가, (5) 사이드포드 중간 station 추가, (6) hull 단면을 6점에서 8점으로 확장, (7) sidepod 단면을 6점에서 8점으로 확장, (8) 차체 외피를 smooth normal로 전환, (9) cockpit shoulder fairing 추가, (10) 앞 coaming 외피 추가, (11) 탄소 coaming lip 추가, (12) steering wheel paddle 추가, (13) mirror stay 보강, (14) intake lip 추가, (15) 상부 배기 fairing 추가, (16) pylon nose fairing 추가, (17) 전방 outer endplate 하부 fence 추가, (18) 중앙 upper flap 추가, (19) diffuser exit plane 추가, (20) 각 변경 후 디자인 스튜디오의 정면 3/4 구도에서 차체 연결부를 재검토한다.

**이유:** 기존 hero 모델은 개별 부품은 있었지만 육각 단면·평면 재질·사각 연결부 때문에 전면 윙, 콕핏, 포드가 서로 분리된 뼈대처럼 읽혔다. 이번 반복은 RB8의 특징을 그대로 복제하는 것이 아니라, 스텝 노즈에서 모노코크·콕핏·언더컷 포드·상부 배기·다층 윙으로 이어지는 2012년형의 연속적인 공력 외피를 만드는 데 집중한다.

**경계:** `LowPolyCar`의 hero 렌더 전용 geometry만 변경한다. `VehicleControlInput`, 120 Hz 물리, Rapier, AI, 카메라의 물리 포즈 및 런타임 외부 자산은 변경하지 않는다. Red Bull 이름·로고·스폰서·실제 리버리·팀별 버튼 배열은 사용하지 않는다.

**검증:** `git diff --check`, 타입 검사, 디자인 화면 E2E, 전체 `npm run verify`, 그리고 디자인 화면의 정면 3/4 수동 시각 검토를 완료 기준으로 사용한다.

## 2026-07-30 — D-049

**결정:** RB8 형상 연구의 전방·콕핏 판독성을 보강한다. 프런트 윙에는 낮은 twin pylon, 외측 cascade flap, 상단 endplate fin을 추가하고, 콕핏에는 좌우 safety shoulder·낮은 시트 입구·후방 headrest를 추가한다.

**이유:** 디자인 화면에서 프런트 윙이 얇은 평판 묶음으로, 드라이버가 안전 셀과 분리된 물체로 보였다. 2012년 오픈휠의 노즈-윙 지지 구조와 콕핏의 impact protection volume을 분리해 보여야 RB8 기준의 기계적 밀도와 착좌 관계를 읽을 수 있다.

**경계:** geometry는 렌더 전용이며 차량 물리·입력·AI·Rapier 차체를 변경하지 않는다. 콕핏의 등받이·headrest는 1인칭 카메라 뒤에 배치해 주행 시야를 막지 않는다. 실제 Red Bull 로고·리버리·팀별 상세 판 형상은 사용하지 않는다.

**검증:** 디자인 화면에서 프런트 윙 pylon·cascade·endplate와 콕핏 safety shoulder·headrest를 시각 확인하고, `npm run verify`와 전체 E2E를 완료 기준으로 사용한다.

## 2026-07-30 — D-048

**결정:** 사용자가 지정한 RB8을 `S1 2012 Open-Wheel`의 형상 대조 기준 차량으로 고정한다. 디자인 화면은 `RB8 Form Study`로 표기하고, 스텝 노즈 냉각 slot·상부 배기 하향 ramp·ramp 아래 sidepod channel·후방 diffuser strake를 렌더 전용으로 반영한다.

**이유:** 2012 시즌 공통형만으로는 구체적인 비율·후방 패키지의 우선순위를 정하기 어려웠다. RB8은 스텝 노즈의 냉각 개구부와 상부 배기·독립 언더컷 채널의 조합이 명확해, 현 디자인 화면의 형상 검토 기준으로 적합하다.

**경계:** 이는 특정 차량의 기능·성능·라이선스 자산을 복제한다는 의미가 아니다. Red Bull 명칭·로고·스폰서·실제 리버리·드라이버 식별은 런타임에 넣지 않는다. 기본 슬레이트 블루 도장은 형상 대비만 위한 S1 무표식 팔레트이며, 물리·Rapier·AI·입력에는 영향을 주지 않는다.

**근거:** [F1Technical RB8 기술 프로필](https://www.f1technical.net/f1db/cars/986/red-bull-rb8)은 RB8의 스텝 노즈와 냉각 aperture를, [RaceFans RB8 배기 분석](https://www.racefans.net/2012/03/14/red-bull-rb8-exhaust-blown-diffuser-diagram/)은 배기 exit·하향 channel·사이드포드 아래 duct의 관계를 설명한다. 로컬 `rb8-jerez-rear-2012.jpg`는 리어 윙·빔 윙·디퓨저 층을 대조한다.

**검증:** 디자인 모드의 전방 3/4·후면 시점에서 RB8 형상 연구 요소를 대조하고, `npm run verify`를 완료 기준으로 사용한다.

## 2026-07-30 — D-047

**결정:** `S1 2012 Open-Wheel` hero 모델은 2012년형 공통 형상 기준을 더 강하게 적용한다. 13인치 휠의 두꺼운 sidewall·작은 림·방사형 스포크·센터 너트를 추가하고, 사이드포드 흡입구·언더컷 채널을 분리한다.

**이유:** 디자인 화면 재검토에서 기존 타이어·허브가 단순한 원통으로, 사이드포드가 넓은 단색 블록으로 읽혀 실제 2012년형 오픈휠의 기계적 밀도와 공력적 음영이 부족했다. 로컬 W03 전체·F2012 전륜·MP4-27 배기 레퍼런스의 휠, 흡입구, 바닥 위 빈 공간 관계를 무표식 geometry로 번역한다.

**경계:** 팀 로고·스폰서·리버리·차종별 판 형상은 복제하지 않는다. 본 변경은 영웅 렌더 모델만 다루며 `VehicleControlInput`, Rapier 차체, 타이어 힘, AI 및 물리 치수에는 영향을 주지 않는다. 모든 수치는 렌더 전용 `initial_assumption`이다.

**검증:** 차 디자인 기본 전방 3/4 시점에서 휠 허브·흡입구·언더컷·다층 윙을 시각 대조하고, 디자인 모드 E2E와 `npm run verify`를 완료 기준으로 사용한다.

## 2026-07-30 — D-046

**결정:** `S1 2012 Open-Wheel`의 hero 외관은 정면 3/4 스튜디오 구도를 기준으로 재대조한다. 사이드포드 폭·높이를 줄여 언더컷과 후방 수축을 드러내고, 바지보드·낮은 롤 후프·타원형 에어박스·얇아진 윙 끝판을 렌더 전용 geometry로 추가한다.

**이유:** 이전 기본 구도는 리어 윙과 원뿔형 에어박스를 과대하게 보이게 해 차량이 2012 시즌의 길고 낮은 오픈휠 비율보다 단순 블록 모델로 읽혔다. 저장된 W03 전체 사진과 F2012 콕핏 레퍼런스의 공통 관계인 낮은 헬멧 노출, 좁아지는 차체 후방, 전방 언더컷을 우선 적용한다.

**경계:** 수정은 `LowPolyCar` hero geometry와 디자인 스튜디오 카메라뿐이다. 특정 팀의 차체 판 형상·리버리·로고는 복제하지 않으며, 차량 물리·Rapier 충돌·휠 축·AI·입력에는 영향을 주지 않는다. 수치는 `initial_assumption`이다.

**검증:** 디자인 모드의 정면 3/4 화면에서 노즈·사이드포드·콕핏·리어 윙의 분리 판독을 시각 확인하고, `npm run verify`로 기존 주행·훈련·레이스 흐름을 회귀 검증한다.

## 2026-07-30 — D-045

**결정:** Driving 화면에 `추적 시점`과 `콕핏뷰` 선택을 추가한다. 기본값은 기존 추적 시점이며, 콕핏뷰는 차량 로컬 중심보다 뒤쪽 0.25 m·차체 기준 높이 1.00 m에서 전방 18 m와 아래 0.35 m를 바라보는 렌더 전용 `initial_assumption`으로 둔다. 콕핏뷰의 FOV는 68°, 추적 시점은 기존 55°를 유지한다. 콕핏뷰에서는 플레이어 자신의 외부 드라이버 메시만 숨긴다.

**이유:** 2012년 레퍼런스에서 운전자는 낮게 앉고, 스티어링 휠은 화면 하단 중앙, 노즈·프런트 윙은 전방 하단에 일부 노출된다. 추적 카메라만으로는 이 시대감과 스티어링·노즈의 관계를 체감하기 어려우므로, 실제 온보드 시야를 해석한 별도 렌더 시점을 제공한다.

**범위:** `App`이 UI 시점 상태를 소유하고 `DrivingScene`은 해당 선택에 맞춰 Three.js 카메라 위치·FOV만 갱신한다. 콕핏 시점은 Rapier의 현재 차체 높이를 읽어 차량 내부와 카메라가 분리돼 보이지 않게 하지만, 차량 포즈·속도·타이어 힘·입력·AI에는 쓰지 않는다.

**검증:** 주행 화면에서 기본 추적 시점, 콕핏뷰 선택 후 `COCKPIT VIEW` 라벨, 재전환을 Playwright로 검증한다. 전체 `npm run verify`를 완료 게이트로 사용한다.

## 2026-07-30 — D-044

**결정:** 콕핏뷰는 외관 레퍼런스와 별도의 개발 기준으로 추가한다. 라이선스가 확인된 2012년 콕핏·스티어링 휠 사진 3개를 `docs/reference-images/f1-2012/`에 저장하고, 공식 Formula1.com 온보드 영상과 Mercedes W03 onboard 페이지는 링크로만 기록한다.

**이유:** 기존 모델은 헬멧·바이저·팔·스티어링 휠을 표시하지만 실제 2012년 운전자 시야에서 보이는 낮은 착좌점, 휠의 화면 하단 위치, 노즈·프런트 윙의 노출 비율을 검증할 기준이 부족했다. 정지 사진은 콕핏 부품 관계를, 온보드 영상은 시야 변화·제동·turn-in·손과 휠의 동작을 보완한다.

**범위:** F2012 콕핏 사진, Lotus E20 스티어링 휠, Caterham 2012 스티어링 휠을 개발용 레퍼런스로 등록한다. S1에는 특정 팀 휠 버튼명·로고·리버리를 복사하지 않고, 낮은 카메라 높이·헬멧 중심·넓은 손잡이·상단 표시부·버튼 밀도만 무표식 geometry와 렌더 카메라 기준으로 재해석한다.

**경계:** Formula1.com·Top Gear 영상과 기사는 저장소에 다운로드하지 않는다. 콕핏뷰는 렌더 전용 카메라·드라이버 상태이며 `VehicleControlInput`, 120Hz fixed-step, 차량 포즈·타이어 힘·AI 제어를 변경하지 않는다.

**검증:** 세 로컬 이미지의 JPEG 형식·SHA-256·개별 Commons 라이선스, 공식 온보드 링크 보존, `LowPolyCar`의 헬멧·휠·카메라 경계와 문서 연결을 확인한다. 변경 후 `npm run verify`를 실행한다.

## 2026-07-30 — D-043

**결정:** 사용자가 제공한 [F1 2012 Google 이미지 검색](https://www.google.com/search?udm=2&q=f1+2012)은 시각적 탐색 인덱스로 보존하고, 실제 개발 비교에는 개별 라이선스가 확인된 2012 시즌 사진 6개를 `docs/reference-images/f1-2012/`에 저장한다. 파일별 출처·작성자·라이선스·SHA-256·관찰 목적은 해당 폴더의 README와 `docs/ASSET_LICENSE_REGISTER.md`에서 관리한다.

**이유:** Google 검색 결과는 원본 파일의 저작권·라이선스·변경 조건을 일관되게 보장하지 않는다. 반면 Commons 개별 File 페이지는 파일별 작성자·라이선스·출처를 확인할 수 있으므로, 개발자가 같은 기준 이미지를 반복해서 확인하면서도 출처를 재현할 수 있다. 로컬 패키지를 만들면 외부 검색 결과가 바뀌어도 노즈·서스펜션·배기·리어 에어로 비교 기준이 유지된다.

**범위:** RB8 후면, MP4-27 배기, Ferrari F2012 배기·전륜 서스펜션, Mercedes W03 전체·전륜 서스펜션 이미지를 개발 문서 자산으로 추가한다. 1280px 축소본은 크롭·색상 보정 없이 사용한다. 사진은 `LowPolyCar`의 공통 2012년형 geometry 관찰에만 사용하고, 실제 팀 로고·스폰서·드라이버명·리버리를 런타임에 복제하지 않는다.

**경계:** 이미지는 `src/`나 production 번들에 import하지 않는다. 투시 사진은 치수 도면이나 물리 파라미터의 근거로 사용하지 않으며, 물리·`VehicleControlInput`·120Hz fixed-step·AI 포즈 소유권은 변경하지 않는다. 상업적 사용이 필요한 경우에도 각 파일의 CC BY/CC BY-SA 조건과 귀속·동일조건 공유 의무를 별도로 준수한다.

**검증:** `docs/reference-images/f1-2012/`의 6개 파일 존재·JPEG 형식·체크섬, 자산 등록부의 개별 링크, Google 탐색 링크 보존, `src/` 참조 부재를 확인한다. 문서·자산 변경 후 `npm run architecture:check`와 전체 `npm run verify`를 실행한다.

## 2026-07-29 — D-042

**결정:** 차량 외관을 집중 확인할 수 있도록 최상위 모드에 `차 디자인`을 추가한다. 별도 R3F `DesignStudioScene`은 `LowPolyCar`의 영웅 외관을 읽기 전용으로 표시하고, DOM `DesignStudioPanel`은 OrbitControls 기반 회전·확대·축소 안내, 정면·측면·후면 시점, 무표식 도장 프리셋, 앞바퀴 조향 미리보기, 자동 회전을 제공한다.

**이유:** 주행 카메라와 AI 추적 카메라는 차량 전체 실루엣과 부품 층을 정지 상태로 비교하기 어렵다. 디자인 검토를 물리 장면에서 분리하면 사용자는 노즈·콕핏·사이드포드·리어 에어로를 직접 확인할 수 있고, 검토용 조향각·색상은 차량 물리나 입력 상태를 오염시키지 않는다.

**범위:** `src/app/DesignStudioScene.tsx`, `src/app/DesignStudioPanel.tsx`, `src/app/App.tsx`, `src/styles.css`, 디자인 모드 E2E를 포함한다. 새 외부 의존성은 추가하지 않고 기존 `@react-three/drei`의 `OrbitControls`·`PerspectiveCamera`를 사용한다.

**검증:** 디자인 모드에서 3D 차량·시점 버튼·도장 프리셋·외관 요소 설명·읽기 전용 경계를 확인하고, 전체 `npm run verify`에서 21개 E2E를 통과시킨다.

## 2026-07-29 — D-041

**결정:** 사용자가 제공한 F1 2012 이미지 검색 결과와 시즌 노즈 비교 자료를 재대조해, `LowPolyCar`를 단일 둥근 저폴리 차체가 아닌 낮은 노즈·높은 모노코크·다층 프런트 윙·강한 사이드포드 언더컷·좁아지는 엔진 커버·넓은 후륜의 공통 2012년형 외관으로 보정한다. 단일 차량 Training은 `hero` LOD를 사용하고 Race Weekend 다차량은 `grid` LOD를 유지한다.

**이유:** 초기 모델은 부품은 존재했지만 상면과 후방 시점에서 2012년 F1 특유의 층과 폭 대비보다 둥근 스포츠카로 읽혔다. 2012년 그리드는 대부분의 스텝 노즈와 차종별 로우 노즈 예외, 높은 프런트 윙 밀도, 깊은 사이드포드 언더컷, 큰 후륜을 공통적으로 보여 주므로 이 관계를 우선 보정해야 한다.

**범위:** `src/world/LowPolyCar.tsx`의 렌더 전용 station·패널·휠 비율, `src/app/TrainingScene.tsx`의 단일 차량 LOD, 디자인 참조·그래픽 계획 문서를 포함한다. 물리 트랙 폭·차량 질량·타이어 힘·입력·AI 제어 경계는 변경하지 않는다.

**근거:** [2012 Formula One front-end comparison](https://gurpzf1.wordpress.com/2012/04/09/front-end-comparison/), [McLaren MP4-27와 Ferrari F2012 비교](https://www.ausmotive.com/2012/02/04/mclaren-mp4-27-v-ferrari-f2012.html), [McLaren MP4-27 기술 공개 자료](https://www.ausmotive.com/2012/02/01/mclaren-unveils-2012-f1-car.html). 실제 사진·로고·스폰서·리버리는 런타임 자산으로 복제하지 않는다.

**검증:** `npm run typecheck`, `git diff --check`, 브라우저 Training·Driving 화면 확인, 기존 차량 물리 집중 테스트와 전체 `npm run verify`를 실행한다. 외관 치수·후륜 폭·핀 높이는 렌더 전용 `initial_assumption`이다.

## 2026-07-29 — D-040

**결정:** 차량 렌더 스냅샷은 물리 위치·속도와 별도로 네 바퀴의 `wheelSpinRad`를 제공한다. 앞바퀴 조향은 부모 그룹의 Y축 회전, 구름은 자식 그룹의 X축 회전으로 분리한다. Rapier 경로에서는 네 바퀴 각속도를 우선 사용하고 순수 AI 교육 경로에서는 속도·휠 반지름 fallback을 사용한다.

**이유:** 기존 앞축 조향은 표시됐지만 네 바퀴의 구름이 렌더링되지 않아 차량이 지면 위에서 미끄러지는 모형처럼 보였다. 물리 포즈를 렌더러가 직접 수정하면 입력·물리 소유권이 깨지므로, fixed-step에서 만든 읽기 전용 회전 상태를 스냅샷으로 전달하는 방식을 선택한다.

**범위:** `VehicleSimulation`의 렌더 스냅샷·외부 Rapier 포즈 계약, `LowPolyCar`의 네 바퀴 회전 그룹, Driving·Training·Race Weekend 연결과 회귀 테스트를 포함한다. 바퀴 회전은 시각 상태이며 타이어 힘·접지·차체 위치를 직접 변경하지 않는다.

**검증:** `VehicleSimulation.test.ts`의 전진·역방향 회전 부호, 물리 집중 테스트, Driving·Training·Race Weekend 렌더 확인, 키보드 가속·조향 E2E를 사용한다. Rapier 초기화 deprecated 경고와 기존 Race Weekend 결과 전환 타이밍 이슈는 별도 위험으로 기록한다.

## 2026-07-29 — D-038

**결정:** M5의 replay는 `VehicleControlInput`을 매 120Hz fixed-step마다 복사하고 입력 직후 `createRaceDeterminismDigest`를 기록하는 순수 `RaceReplay` 모듈로 고정한다. JSON schema는 `s1-racing-replay-v1`로 버전 관리하며 Race Weekend UI는 완료된 recording만 저장하고, 불러온 파일은 현재 트랙·랩·참가자 수와 먼저 대조한다.

**이유:** 렌더 프레임이나 React 상태를 기록하면 브라우저 성능·UI 타이밍이 replay 결과를 오염시킨다. 물리 계층과 같은 입력 경계를 그대로 기록하고 성능 측정값을 제외한 digest를 비교해야, M4 운영 상태까지 포함한 결정성 회귀와 파일 변조를 fixed-step 단위로 재현할 수 있다.

**범위:** `RaceReplayRecorder`, JSON serialize/parse/validate, step별·최종 digest 검증, Race Weekend 저장·불러오기 UI와 단위·E2E를 포함한다. 독립 재생에 필요한 차량 정의·AI 설정 manifest, 온라인 동기화, 연료·DRS·KERS·날씨는 M6 이후로 남긴다.

**검증:** `RaceReplay.test.ts`, `RaceWeekendSession.test.ts`, Race Weekend 저장·불러오기 E2E, `npm run architecture:check`, `npm run verify`와 Codex 앱 브라우저 실제 플레이를 완료 게이트로 사용한다.

## 2026-07-29 — D-037

**결정:** M4는 공유 `RaceCollisionWorld`, 데이터 기반 `pitLane`, 순수 `RaceRegulations` 세 경계로 구현한다. `RaceSession`은 공통 `VehicleControlInput`과 `VehicleSimulation`을 계속 소유하고, Rapier 공유 세계는 다차량 cuboid 차체의 shape solver 후 포즈·접촉 이벤트만 반환한다. 피트 레인은 본선과 같은 트랙 데이터에서 중심선·게이트·박스·속도 제한을 읽으며, 규정 엔진은 황색기·세이프티카·레드 플래그·시간 패널티를 상태 스냅샷으로 제공한다.

**이유:** 기존 M3A 원형 접촉은 결정성 검증에는 충분하지만 실제 차체 형상과 yaw 회전을 표현하지 못했고, M3D의 피트 서비스는 위치·속도와 분리된 추상 정지였다. 세 기능을 RaceSession이나 렌더러에 직접 하드코딩하면 물리 소유권과 트랙 원본이 다시 분리되므로, 구현 가능한 단위 경계를 먼저 고정한다.

**범위:** `RaceCollisionWorld`/`RapierMultiCarCollision`, `PitLaneMonitor`, `RaceRegulations`, `RaceSession` 연결, 피트 레인 트랙 원본·시각화·Race Weekend M4 상태 표시와 단위·통합 테스트를 포함한다. 전체 FIA 규정, 온라인 동기화, 연료·DRS·KERS·날씨는 포함하지 않는다.

**검증:** M4 전용 Rapier shape contact, RaceSession 주입, 피트 게이트·박스·속도 위반, 규정 플래그 단위 테스트와 전체 `npm run verify`를 사용한다. 트랙·속도·패널티·차체 치수는 실제 경기 또는 차량 재현값이 아닌 `initial_assumption`이다.

## 2026-07-29 — D-036

**결정:** 차량 움직임을 2012 F1의 공통 동역학에 가깝게 조정하기 위해, FIA 규정으로 확인되는 640 kg·2.4 L V8·18,000 rpm·7단·휠 치수 기준과 공식 2012 온보드 자료의 조작 흐름을 기준선으로 사용한다. 공력 계수·타이어 곡선·관성·서스펜션 강성은 특정 팀의 실차 데이터가 아닌 `initial_assumption`/`simulation_required`로 표시한다.

**이유:** 기존 차량은 780 kg·8,000 rpm·낮은 다운포스와 전후 횡강성 불균형으로, 고속에서 차체가 붙기보다 저속과 고속의 차이가 약하고 turn-in이 인공적으로 느껴졌다. 2012 F1다운 주행감은 고속 공력 하중, 작은 슬립각, 빠른 yaw 응답, 강한 제동과 trail-braking 전이를 함께 만들어야 한다.

**범위:** `AeroModel`, `TireModel`, `VehiclePhysics`, `RapierChassisSuspension`의 기본 기준값과 `SingleOpponentAI`의 예측 제동·카운터스티어 기준값, 관련 회귀 테스트·문서를 변경한다. `VehicleControlInput`, AI가 위치를 직접 바꾸지 않는 경계, 120Hz fixed-step, 렌더링 스냅샷 소유권은 유지한다. KERS·DRS·연료 질량 변화는 후속 마일스톤으로 남긴다.

**검증:** `PhysicsValidation`, `VehiclePhysics`, `RapierChassisSuspension`, `SingleOpponentAI`, AI 고속 복합·전체 랩 평가와 브라우저 Driving·AI Training 화면을 확인하고 전체 `npm run verify`를 완료 게이트로 사용한다. F1 2012 출처와 영상 확인 대상은 `docs/F1_2012_DYNAMICS_REFERENCE.md`에 기록한다.

## 2026-07-29 — D-035

**결정:** 앞축 타이어의 시각 조향은 `VehicleRenderSnapshot.steeringAngleRad`를 통해 렌더러에 전달하고, 차체 yaw와 분리된 앞바퀴 그룹만 회전시킨다. 영웅 차량의 오픈 콕핏에는 외부 자산 없이 저폴리 헬멧·바이저·상체·팔·장갑으로 구성한 드라이버를 표시하고, 다차량 grid LOD에서는 비용을 늘리지 않는다.

**이유:** 기존 차량은 타이어가 항상 차량 로컬 방향을 유지해 조향 입력이 화면에서 보이지 않았고, 오픈 콕핏은 비어 보였다. 조향각을 렌더 스냅샷으로 확장하면 물리 계층은 그대로 두면서 Driving·Training·Race Weekend가 같은 시각 계약을 사용할 수 있다. 드라이버는 2012년형 시대감과 콕핏 판독성을 높이되 외부 팀·드라이버 자산을 복제하지 않는다.

**범위:** `VehicleSimulation`의 읽기 전용 렌더 스냅샷, `LowPolyCar`의 앞축 그룹과 hero cockpit, Driving·Training·Race Weekend의 연결 코드와 회귀 테스트만 변경한다. 조향 입력, 타이어 힘, 차량 포즈, AI 제어 경계는 변경하지 않는다.

**검증:** `VehicleSimulation.test.ts`에서 조향각 전달을 확인하고, `npm run verify`에서 타입·단위·아키텍처·빌드·브라우저 검사를 실행한다. 화면에서는 Driving·Training·Race Weekend에서 앞축이 차체 yaw와 독립적으로 회전하고 hero 콕핏에 드라이버가 보이는지 확인한다.

## 2026-07-29 — D-034

**결정:** `design-taste-frontend`는 S1 Racing의 게임 HUD와 모드 전환 셸에만 적용한다. 기존 React/CSS 구조 안에서 `--ui-*` 토큰, Pit Wall / Telemetry 색상 계층, 4px·6px 각진 패널, 민트 기본 액센트, 의미 기반 황색·적색 상태를 사용한다. Tailwind·Motion·아이콘 라이브러리는 추가하지 않는다.

**이유:** 이 화면의 핵심은 마케팅용 시각 연출이 아니라 주행 중 텔레메트리와 운영 상태의 판독성이다. 새 UI 의존성을 도입하면 번들·상태 경계·검증 범위가 불필요하게 커진다. 기존 HUD의 기능과 ARIA 경계를 보존하면서 공통 토큰과 포커스·reduced motion 규칙을 적용하는 편이 현재 마일스톤에 적합하다.

**범위:** `src/styles.css`의 공통 셸·HUD·Training·Race Weekend 스타일과 `docs/DESIGN_TASTE_FRONTEND_REDESIGN.md`를 갱신한다. 차량 물리, 120Hz fixed-step, `VehicleControlInput`, AI 입력, Race Weekend 상태 전이와 캔버스 렌더링 소유권은 변경하지 않는다.

**검증:** `npm run typecheck`, `npm run test:unit`, `npm run architecture:check`, `npm run build`, `npm run test:e2e`를 포함한 `npm run verify`를 실행한다. 기존 E2E 타이밍 실패가 재현되면 CSS 변경과 분리해 기록한다.

## 2026-07-29 — D-033

**결정:** F1 2012 차량 외관의 98% 유사도 요청은 특정 팀 차량의 복제가 아닌 2012년형 공통 형상 기준으로 구현한다. 공통 `LowPolyCar`는 물리 기준 휠베이스 3.30 m와 트랙 폭 1.60 m에 시각 휠 축을 정렬하고, 스텝 노즈·다층 프런트 윙·오픈 콕핏·사이드포드 언더컷·에어박스·고후방 배기·리어 윙/빔 윙/디퓨저·push-rod/pull-rod를 표시한다.

**이유:** 사용자가 특정 팀 또는 한 대의 차종을 지정하지 않았으므로 단일 차량의 픽셀 유사도를 객관적으로 보장할 수 없다. 대신 2012년 시즌을 대표하는 공통 기술 문법을 높은 충실도로 보존해야 시즌 시대감과 독창적인 S1 자산 경계를 동시에 유지할 수 있다. 실제 팀 로고·스폰서·드라이버·리버리는 사용하지 않는다.

**검증:** `src/world/LowPolyCar.tsx`의 station 기반 차체와 공력·기계 요소를 정면·측면·후면 기준으로 검토하고, `npm run verify`에서 타입·단위·빌드·브라우저 검사를 실행한다. 형상 수치는 시각 렌더링 전용 `initial_assumption`이며 차량 물리 설정을 대체하지 않는다.

## 2026-07-29 — D-032

**결정:** F1 2012 차량 이미지·기술 자료는 프로젝트 문서와 연결된 Notion 참조 페이지에 함께 기록한다. Notion의 `11. 외부 자료 조사 결과 — 공식 기준값과 구현 결정` 하위에 `13. F1 2012 차량 디자인 참조 자료` 섹션을 두고, 차종별 이미지 갤러리·기술 프로필·규정 근거·S1 적용 경계를 유지한다.

**이유:** 차량 디자인 조사는 이미지, 기술 설명, 규정, 라이선스 판단을 함께 반복해서 확인해야 하므로 저장소 문서만으로 관리하면 시각 자료 탐색 경로가 분산된다. Notion 허브에 조사 결과를 연결하면 다음 디자인 세션에서 같은 자료를 다시 찾을 수 있고, 저장소에는 재현 가능한 링크와 라이선스 경계를 남길 수 있다. 외부 사진은 현재 런타임 자산으로 복사하지 않는다.

**검증:** Notion 페이지의 부모가 `S1 Racing — 공식 문서·수학 공식·참고자료 허브`인지 다시 조회하고, F1 2012 섹션에 공통 규정·12개 차종 링크·비복제 규칙이 저장되었는지 확인한다. 저장소에서는 `docs/F1_2012_DESIGN_REFERENCE.md`와 `docs/ASSET_LICENSE_REGISTER.md`의 링크·참조 전용 상태를 검토한다.

## 2026-07-29 — D-032

**결정:** 성능 개선은 물리 120Hz와 `VehicleControlInput` 경계를 유지한 채 Training·Race Weekend에 `FixedTimestepAccumulator`를 적용하고, Race Weekend의 AI 차량 그림자·Rapier CCD·collider 선형 탐색을 줄인다. 일반 주행 화면은 모드별 WebGL 장면을 lazy load하며 Canvas는 DPR 1.25, basic shadow, antialias 비활성화를 기본으로 한다.

**이유:** 프레임마다 고정 2-step 또는 `ceil(delta * 120)`을 실행하면 화면 FPS가 물리 시간과 CPU 부하를 결정하고, 20대 AI의 shadow map과 CCD는 주행감과 무관하게 GPU·solver 비용을 키운다. 카메라가 전체 그리드를 매 프레임 복사하는 경로도 10Hz HUD 경계와 맞지 않는다. 수정 범위는 렌더·충돌 비용만 제한하며 AI의 입력, 차량 포즈 소유권, 리플레이 120Hz 기록 계약을 바꾸지 않는다.

**검증:** `FixedTimestep.test.ts`, `RaceSession.test.ts`, `RapierMultiCarCollision.test.ts`, 전체 `npm run verify`, 브라우저 Training·Driving·Race Weekend E2E를 실행한다. `npm run review:code`와 `npm run review:performance`를 `verify`와 GitHub Actions에 넣어 이후 모든 코드 변경에서 공백·도메인 경계·debug 출력·시간 루프·Canvas·Rapier·번들 예산을 자동 검토한다. JavaScript 예산은 전체 4,000,000 B 및 단일 chunk 2,500,000 B로 시작한다. 모드 분할 후 최대 chunk는 Rapier lazy chunk 2,235,460 B였으며, 변경은 실제 프로파일 근거와 함께 이 문서에 기록한다.

## 2026-07-29 — D-031

**결정:** 저폴리 그래픽 1차 재구성은 `VisualPalette`와 `SceneLighting`을 공통 렌더 모듈로 두고, Training·Driving·Race Weekend는 동일한 트랙 색상·노을 환경광·안개 규칙을 사용한다. 카메라 거리와 HUD 폭만 장면 목적에 맞게 조정하며 물리 스냅샷·입력·AI 경계는 변경하지 않는다.

**이유:** 장면마다 배경·조명·재질 색을 직접 수정하면 같은 트랙이 모드에 따라 서로 다른 게임처럼 보이고, HUD가 시야를 덮는 문제를 개별 화면에서 반복 수정하게 된다. 공통 토큰과 제한된 광원 수를 사용하면 저폴리 면 대비를 유지하면서 시각 변경의 범위와 성능 비용을 예측할 수 있다. 도시형 배경과 외부 자산은 추가하지 않는다.

**검증:** `npm run typecheck`, `npm run architecture:check`, `npm run verify`와 Codex 앱 브라우저의 Training·Driving·Race Weekend 초기 화면 캡처를 사용한다. 색상·조명·카메라 수치는 실제 트랙 재현값이 아닌 `initial_assumption`이다.

## 2026-07-28 — D-029

**결정:** 1차 완성의 오프라인 레이스 운영 범위는 `TyreCondition`·`RacecraftStateMachine`·`RaceOperations` 순수 모듈로 분리하고, `RaceSession`은 이들의 스냅샷과 `VehicleSimulation` fixed-step 순서를 조정한다. 결정성 digest는 렌더 성능 측정값을 제외한 물리·전략·운영 상태만 직렬화한다.

**이유:** 타이어·AI 의도·손상·피트 시간을 React/R3F 또는 Rapier 장면에 직접 넣으면 단위 테스트와 리플레이 경계가 흐려진다. 순수 모듈을 유지하면 기존 M3A 접촉 근사와 공통 `VehicleControlInput`을 보존하면서 1차 완성 기능을 같은 120Hz 규칙으로 검증할 수 있다.

**검증:** `TyreCondition.test.ts`, `Racecraft.test.ts`, `RaceOperations.test.ts`, `RaceSession.test.ts`, Race Weekend HUD E2E와 `npm run verify`를 사용한다. 타이어·손상·피트 수치는 실차 규정값이 아닌 `initial_assumption`이다.

## 2026-07-28 — D-030

**결정:** M3D의 피트 정차는 전략 랩 진입 시 컴파운드를 교체하고 2.5초 initial_assumption 동안 해당 참가자의 물리 입력을 보류한다. 실제 피트 레인 차선과 속도 제한은 1차 완성에서 제외한다.

**이유:** 최소 전략이 실제 레이스 시간에 영향을 주는지 검증하면서도, 아직 피트 레인 지오메트리를 도입해 차량 물리·카메라·트랙 경계를 동시에 변경하지 않기 위해서다.

**검증:** `RaceOperations.test.ts`에서 서비스 진행·완료를, RaceSession 스냅샷에서 피트 상태와 타이어 교체를, Race Weekend E2E에서 운영 HUD를 확인한다.

## 2026-07-27 — D-028

**결정:** M3A의 트랙 경계는 TestTrackDefinition의 정적 벽·연석 선분을 단일 원본으로 두고, 렌더링과 Rapier가 같은 선분을 각각 메시와 fixed collider로 변환한다. 트랙 리밋·랩 유효성은 순수 TrackLimitsMonitor가 소유하고, RaceSession의 다차량 접촉은 초기에는 순수 2D 원형 근사 응답으로 분리한다.

**이유:** 트랙 화면, 노면 샘플러, 충돌 월드가 서로 다른 좌표를 가지면 이탈 판정과 벽 접촉을 재현할 수 없다. 반면 여러 차량을 Rapier 하나의 world에 즉시 넣으면 기존 단일 차량 물리의 소유권과 회귀 기준이 동시에 바뀐다. M3A에서는 규칙의 결정성과 차량 간 침투·반발 경계를 먼저 검증하고, 차체 형상·회전·손상은 이후 별도 마일스톤에서 확장한다.

**검증:** TrackLimits.test.ts, VehicleContact.test.ts, RaceSession.test.ts, RapierChassisSuspension.test.ts와 M3A HUD·Race Weekend E2E를 사용한다. 벽·연석 치수와 패널티는 initial_assumption이며 실차 규정 또는 특정 차량 재현값으로 취급하지 않는다.

## 2026-07-27 — D-026

**결정:** M2B~M2D는 `RaceSession`·`QualifyingSession`·`RaceWeekendSession`으로 분리한다. `RaceSession`은 차량별 `VehicleSimulation`과 fixed-step 순위만 소유하고, `QualifyingSession`은 유효 랩·Q 컷만 계산하며, `RaceWeekendSession`은 단계 전이와 최소 전략만 조정한다.

**이유:** 다차량 물리·랩타이밍·주말 운영을 하나의 장면 컴포넌트에 넣으면 AI 입력 경계와 실패 원인을 분리하기 어렵다. 순수 TypeScript 상태 기계로 두면 React/R3F를 거치지 않고 결정성·리셋·컷 규칙을 테스트할 수 있고, UI는 읽기 전용 스냅샷만 표시한다.

**검증:** `RaceSession.test.ts`, `QualifyingSession.test.ts`, `RaceWeekendSession.test.ts`에서 그리드 결정성·유효 랩·20→15→10 컷·전략 경계·리셋을 검증하고, Playwright에서 Race Weekend 모드의 퀄리파잉 실행·레이스 시작·20대 순위·리셋을 확인한다.

## 2026-07-27 — D-027

**결정:** M2D의 타이어 전략은 시작 컴파운드와 다른 컴파운드로 마지막 전 랩 이전에 최소 한 번 정지하는 `RaceStrategy` 입력 계약만 고정한다. 열·마모·공기압과 실제 피트 물리는 구현하지 않는다.

**이유:** 전략 선택 UI와 레이스 주말 상태 전이를 먼저 검증하면서, 아직 정의되지 않은 타이어 열·마모 모델이 레이스 순위나 AI 성능에 가짜 보너스를 주는 것을 막기 위해서다. 후속 타이어 마일스톤에서 모델을 추가해도 동일한 전략 인터페이스를 유지할 수 있다.

**검증:** `RaceWeekendSession.test.ts`에서 시작·피트 컴파운드 동일 조건과 랩 범위 오류를 거부하고 유효 전략을 보존하는지 확인한다.

## 2026-07-27 — D-025

**결정:** Training Lab의 자동 튜닝 결과는 `s1-racing.ai-training-result` schema v1 JSON으로 저장한다. 파일에는 완료 에피소드 스냅샷, 기준·최고·후보 설정 평가, 시나리오별 결정성 해시, 자동 적용 여부와 실패 사례를 포함하며, 파일 생성은 앱 셸의 브라우저 다운로드 경계에서만 수행한다.

**이유:** 다음 마일스톤에서 적용된 AI 설정의 출처와 튜닝 전후 근거를 다시 확인하려면 설정만이 아니라 동일 조건의 평가 지표와 해시가 필요하다. 평가 실행기는 React·DOM에 의존하지 않아야 하므로 JSON 구조와 직렬화는 순수 TypeScript로 유지하고, `savedAtUtc`는 비결정적 파일 메타데이터로만 취급한다.

**검증:** `AITrainingResult.test.ts`에서 schema 버전, 설정·해시 보존, 중첩 스냅샷 복사와 JSON 파싱을 검증하고, Training Lab E2E에서 `결과 JSON 저장` 다운로드 파일명을 확인한다.

## 2026-07-24 — D-024

**결정:** AI 고속 코너 제어는 차체 슬립이 0.06 rad(약 3.4°) 미만일 때만 요청 스로틀을 유지한다. 이 한계를 넘으면 AI는 브레이크를 강제하지 않고 스로틀을 lift하며, 실제 속도 벡터 반대 방향의 슬립 복구 조향을 더한다. 자동 튜닝 평가는 이 상한을 넘긴 후보에 미완주와 같은 큰 페널티를 부과해 랩타임이 빨라도 다음 교육에 적용하지 않는다.

**이유:** F1의 고속 코너는 레이싱 라인·에이펙스·트랙 폭을 이용해 회전 반경을 키워 속도를 유지하는 것이며, 풀스로틀은 차체가 안정되고 횡그립 여유가 있을 때의 결과다. Formula 1 공식 용어집도 드리프트를 정상 레이싱 라인보다 느린 상태로 정의한다. 이전 0.12 rad(약 6.9°) 상한은 타이어 작동 슬립을 허용하는 수준을 넘어 화면에서 후미가 흐르는 것으로 보였다. 수치는 실차 재현값이 아닌 `initial_assumption`이며, 차량 위치·속도·그립을 직접 수정하지 않고 공유 `VehicleControlInput`만 바꾼다.

**검증:** `SingleOpponentAI.test.ts`에서 슬립 초과 시 lift·복구 조향을, `AITrainingRunner.test.ts`와 `AITrainingEvaluator.test.ts`에서 고속 복합 코너 최대 슬립 0.06 rad 이하를 검증한다. Training Lab E2E는 고속 시나리오와 HUD의 차체 슬립 한계를 확인한다.

## 2026-07-24 — D-023

**결정:** Training Lab은 도로 리밋 이탈과 월드 맵 이탈을 구분한다. 도로 밖 주행은 기존 이탈 지표로 누적하되, 차량 위치가 트랙 데이터의 `outerBounds`를 벗어나면 같은 fixed step에서 에피소드를 `failed`로 종료한다. 실패 사례에는 위치, 맵 경계 초과 거리, 속도, 횡오차, 구간, 입력을 보존하고 완료 후 자동 튜닝에 전달한다.

**이유:** 도로 폭 바깥의 짧은 오차까지 즉시 중단하면 경계 회복 능력을 평가할 수 없지만, 월드 맵 밖은 정상적인 주행·복구 상태가 아니며 이후 텔레메트리를 오염시킨다. 실패 에피소드는 기존 미완주 패널티와 이탈 지표를 받아 후보 선택에서 불리해지고, HUD는 다음 튜닝이 어떤 문제를 반영했는지 설명한다.

**검증:** `AITrainingRunner.test.ts`에서 `outerBounds` 바깥의 차량이 한 fixed step 뒤 실패하고 실패 스냅샷을 보존하는지 검증한다. Training Lab E2E는 완료·실패 후 자동 튜닝의 사용자 흐름을 계속 검증한다.

## 2026-07-24 — D-022

**결정:** Northfield GP 고속 복합 코너의 차체 횡슬립 상한을 0.12 rad(약 6.9°)로 둔다. 단순 평면 차량 물리의 후륜 코너링 강성은 70,000 N/rad으로 설정해 과도한 후미 회전을 줄이고, 기본 설정과 완료 후 자동 튜닝이 선택한 설정 모두 이 상한을 통과해야 한다.

**이유:** F1 계열 주행은 타이어 작동 슬립각을 사용하지만 차체가 눈에 띄게 옆으로 흐르는 드리프트를 목표로 하지 않는다. 현재 평면 모델은 고속 전환에서 후륜 횡력이 부족해 최대 약 9.4°의 차체 슬립을 만들었다. 후륜 횡강성 보정은 차량 위치를 직접 조작하거나 AI에 숨은 그립을 주지 않고 같은 타이어 힘 경계 안에서 안정성을 높인다. 모든 수치는 `initial_assumption`이며 실차 재현값이 아니다.

**검증:** `AITrainingRunner.test.ts`에서 기본 고속 복합 코너의 최대 차체 슬립각을 검증하고, `AITrainingEvaluator.test.ts`에서 완료 후 자동 튜닝이 선택한 후보도 같은 상한을 통과하는지 검증한다.

## 2026-07-24 — D-021

**결정:** Training Lab에서 별도의 `AI 학습 실행`·`최고 설정 적용` 조작을 제공하지 않는다. 사용자가 새 교육을 시작하면 현재 설정으로 에피소드를 끝까지 실행한다. 완료 또는 실패 뒤에 사용자가 실행한 시나리오의 제한된 결정적 후보를 평가하고, 최고 후보의 점수가 엄격히 더 낮을 때만 설정을 다음 에피소드에 자동 적용한다. 일시정지 재개는 동일 에피소드이므로 재탐색하지 않는다.

**이유:** 교육 시작 전에 탐색하면 사용자가 실제로 관찰한 주행과 학습 결과의 순서가 뒤바뀐다. 완료 후 현재 설정을 baseline으로 사용하면 한 에피소드의 결과를 먼저 관찰하고, 이전 설정보다 좋아지는 경우에만 다음 시도에 반영할 수 있다. 후보 탐색은 물리 실행기와 분리된 결정적 평가기로 유지한다.

**검증:** Training Lab E2E에서 시작·에피소드 완료·후속 자동 적용 결과를 확인하고, 기존 평가기 단위 테스트로 같은 설정의 결과 재현성을 보장한다.

## 2026-07-24 — D-020

**결정:** Training Lab의 전체 랩 퍼센트는 고정 스텝이나 체크포인트 개수가 아니라 차량 위치를 Northfield GP 레이싱 라인에 투영한 누적 거리로 계산한다. 투영 거리의 순환 구간 차분은 부호를 유지하므로 전진하면 증가하고 역주행하면 감소한다. 100%와 교육 완료는 마지막 결승선 재통과 체크포인트를 통과했을 때만 표시한다.

**이유:** 실행 시간이나 불연속 체크포인트를 진행률로 사용하면 차량이 아직 트랙 중간에 있어도 100%가 되거나, 실제 위치 변화가 HUD에 반영되지 않는다. 레이싱 라인 거리 투영은 물리 시뮬레이션이 소유한 차량 상태만 읽어 연속적인 사용자 관찰값을 제공하고, 시작선 경계도 순환 트랙에 맞게 처리한다.

**검증:** `AITrainingRunner.test.ts`에서 초기 시간 제한보다 긴 전체 랩이 100%가 아닌지와 전진·역주행의 부호 차분을 검증하고, Training Lab E2E에서 실제 트랙 진행률·거리·출발선·도착선 UI를 확인한다. 투영은 자기 교차하지 않는 Northfield GP 중심선에 대한 `initial_assumption`이며, 향후 자기 교차 트랙은 구간 방향 추가 판별이 필요하다.

## 2026-07-24 — D-019

**결정:** Training Lab의 고정 월드 카메라를 AI 차량 뒤쪽·위쪽의 추적 카메라로 교체하고, Northfield GP는 긴 고속 가속·강제동·고속 복합·저속 탈출·기술 복귀가 순환하는 대형 중심선으로 확장한다. 실제 Silverstone의 레이아웃, 좌표, 코너명, 시설, 브랜딩은 계속 사용하지 않는다.

**이유:** 고정 카메라는 넓어진 교육 트랙의 시작 지점과 진행 중인 AI를 쉽게 프레임 밖으로 밀어 AI의 조향·제동·차체 자세를 검토하기 어렵게 한다. 추적 카메라는 AI와 전방 레이싱 라인을 함께 보이게 하며, 대형 원본 레이아웃은 단순 원형 반복보다 고속에서 저속으로 전환되는 교육 목표를 명확히 만든다.

**검증:** Northfield 데이터 단위 테스트에서 중심선·경계·목표 속도·제동점을 갱신해 검증하고, Training Lab E2E와 `npm run verify`로 AI 관찰 화면과 전체 완료 게이트를 확인한다. 모든 폭·속도·곡률 값은 `initial_assumption`이며 주행감은 `simulation_required`다.

## 2026-07-24 — D-018

**결정:** M2A-0 교육 트랙은 기존 사각 테스트 루프를 대체하지 않고, `NORTHFIELD_GP_DATA`의 중심선·도로 폭·곡률·섹터·체크포인트·레이싱 라인 데이터로 분리한다. 물리 표면 샘플러는 중심선과 폭을 이용해 도로 경계를 계산하고, Training Lab 디버그 월드는 같은 데이터로 평면 도로 메시를 만든다.

**이유:** 기존 테스트 루프는 M1F 입력·경계 회귀를 위한 안정적인 기준선으로 보존해야 한다. M2A-0은 고속 직선·강제동·고속 복합·중속 코너·저속 탈출의 주행 특성을 독립적으로 평가해야 하므로, 이름만 바꾼 사각 루프가 아닌 별도 지오메트리와 시나리오 시작점을 사용한다.

**검증:** `NorthfieldGP.test.ts`에서 중심선 순서·도로 폭·signed 경계 거리·노면·목표 속도·제동점·결정적 리플레이를 검증하고, Training Lab E2E에서 저속 탈출 시나리오를 실행한다. 수치와 주행감은 `initial_assumption` 및 `simulation_required`로 관리한다.

## 2026-07-23 — D-017

**결정:** 단일 AI 레이스 통합 전에 M2A-0 `AI Training Circuit & Evaluator`를 둔다. 교육은 초기에는 신경망 학습이 아니라 120Hz 결정적 에피소드 실행, 제한된 AI 파라미터 탐색, 텔레메트리 기반 회귀 평가로 구현한다. 교육 트랙은 Silverstone의 고속·기술적 주행 특성만 참고한 독창 레이아웃으로 만들고, 실제 레이아웃·좌표·코너명·시설·브랜딩·외부 자산은 복제하지 않는다.

**이유:** AI 튜닝과 다차량 세션을 동시에 진행하면 물리·제어·세션 운영의 실패 원인을 분리할 수 없다. 결정적 교육기가 있어야 동일한 차량·트랙·AI 설정의 결과를 재현하고, 실제 트랙 복제에 따른 라이선스·브랜딩 위험도 피할 수 있다.

**검증:** 교육 트랙 데이터 단위 테스트, AI 에피소드 결정성 해시, 목표 속도·제동·횡오차·트랙 이탈·입력 채터링 평가, 관련 E2E와 `npm run verify`를 사용한다. 교육 트랙 및 튜닝값은 `initial_assumption`이고 주행감은 `simulation_required`다.

## 2026-07-22 — D-015

**결정:** Milestone 1F의 입력 프리셋과 테스트 트랙은 각각 `BrowserVehicleInput`과 `src/tracks/TestTrack.ts`를 단일 브라우저·데이터 경계로 사용한다. 입력 장치는 `VehicleControlInput`만 출력하고, 물리 표면 샘플러와 R3F 월드 시각화는 같은 트랙 정의를 읽는다.

**이유:** 입력 장치별 처리를 물리 계층에 분산하면 AI·게임패드·휠을 연결할 때 결정성과 검증 책임이 흐려진다. 트랙 형상과 노면 판정을 렌더링 코드와 물리 코드에 중복하면 경계·마커·리셋 위치가 어긋날 수 있다.

**검증:** 게임패드 변속 상승 에지, 휠 축 캘리브레이션, 시작 포즈 리셋, 구간·체크포인트 순서, 외곽 경계 이탈 HUD를 Vitest와 Playwright로 검증한다.

## 2026-07-22 — D-016

**결정:** 공식 `game-studio`의 S1 Racing 적용 규칙을 프로젝트 로컬 스킬 `.agents/skills/s1-racing-game-studio/`로 설치하고, 현재 런타임 스택에는 `@react-three/drei`만 추가한다. React/R3F/Three.js와 직접 연결한 `@dimforge/rapier3d-compat` 브리지는 유지하며, Phaser·PixiJS·Babylon.js·PlayCanvas·Howler·Colyseus는 각 기능의 별도 마일스톤 전까지 추가하지 않는다.

**이유:** 프로젝트 지침과 검증 게이트를 레포에 함께 보존해야 다른 작업 세션에서도 동일한 게임 개발 기준을 재현할 수 있다. `@react-three/drei`는 기존 R3F 구조에 선택적으로 결합되는 로더·카메라·환경 보조 라이브러리인 반면, 다른 엔진이나 멀티플레이·오디오 라이브러리는 현재 Physics Prototype의 범위를 넓히고 마이그레이션 위험을 만든다.

**검증:** 스킬 메타데이터 quick validation, `npm install` 및 `npm ls --depth=0`, `git diff --check`를 통과했다. 최종 `npm run verify`도 타입 검사·단위 테스트 47개·아키텍처 검사·프로덕션 빌드·Playwright E2E 7개를 모두 통과했다. 이번 변경은 게임 모듈 경계를 바꾸지 않으므로 Archify 다이어그램은 갱신하지 않는다.

## 2026-07-22 — D-017

**결정:** S1 Racing은 로직·함수·비자명 변수에 사람 중심의 주석을 적극적으로 사용한다. 주석은 `what` 반복이 아니라 물리 공식·단위·불변식·이벤트 순서·브라우저 제약·성능 선택·거부한 대안의 `why`를 기록하며, export 경계에는 필요한 계약을 TSDoc으로 남긴다.

**이유:** Reddit의 `r/programming`, `r/ExperiencedDevs`, `r/ChatGPTCoding` 논의에서는 자명한 코드를 한 줄씩 해설하는 주석과 AI가 생성한 장황·부정확한 문서를 공통적으로 경계했다. 반면 코드만으로 복원하기 어려운 설계 이유, 가정, 최적화 제약, 유지보수 경고는 코드 가까이에 남겨야 한다는 의견이 반복됐다. 따라서 “주석을 많이”를 줄 수가 아니라 비자명 맥락의 커버리지와 정확성으로 정의한다.

**검증:** 프로젝트 로컬 스킬·AGENTS 규칙·역할 템플릿에 동일한 주석 기준을 반영했다. AI 에이전트는 변경된 로직의 관련 주석을 추가·갱신하고 완료 보고에 그 이유를 기록한다. Reddit 엔진은 샌드박스 DNS 제한으로 결과를 수집하지 못했으므로, 검색 결과의 공개 Reddit 논의와 저장된 보충 조사만 근거로 사용했다.

## 2026-07-19 — D-014

**결정:** S1 Racing의 모델 라우팅은 Lead `gpt-5.6-terra/high`, Writer·Explorer `gpt-5.6-luna/medium`, Physics·QA `gpt-5.6-terra/high`로 구성한다.

**이유:** Lead와 물리·QA는 여러 파일과 검증 결과를 종합하고 경계·회귀를 판단해야 하므로 Terra를 사용한다. Writer와 Explorer는 명확히 bounded된 문서·탐색·기계적 작업의 비용을 줄이기 위해 Luna를 사용한다. 물리 설계처럼 판단 난도가 올라가는 작업은 Writer가 아니라 Physics Worker로 승격한다.

**검증:** 역할별 TOML 템플릿과 모델 라우팅 표를 일치시켰다. 모든 Worker 결과는 기존 QA와 `npm run verify` 게이트를 거치며, 이 설정 변경은 코드 모듈 경계를 바꾸지 않으므로 Archify 다이어그램은 갱신하지 않는다.

## 2026-07-19 — D-013

**결정:** S1 Racing의 기본 오케스트레이션은 Lead가 최종 책임을 유지하는 manager 패턴으로 운영하고, Codex custom agent 설정은 `.codex/agents/*.toml`을 목표 위치로 삼는다. 현재 `.codex/`가 읽기 전용이므로 저장소에는 검토 가능한 TOML 템플릿만 둔다.

**이유:** 공식 Codex 문서는 `AGENTS.md`를 작고 지속적인 규칙에 한정하고, 전문 custom agent는 별도 설정 파일에 좁은 책임과 맞는 도구·권한으로 정의하도록 안내한다. 공식 multi-agent 문서는 독립·bounded 작업에는 병렬화와 분리된 컨텍스트가 유효하지만, 공유 mutable state·순차 의존성·고정 실행 그래프에는 단일 에이전트가 낫다고 설명한다. S1 Racing은 물리·입력·렌더링 공유 경계가 강하므로 이 구분이 우선이다.

**검증:** 탐색·QA는 read-only, 구현은 명시된 파일 소유권, 기본 `max_threads = 4`·`max_depth = 1`, 실패 후 1회 제한 재시도, 최종 `npm run verify` 게이트를 운영 계약에 추가했다. 코드 모듈 경계는 변경하지 않으므로 Archify 다이어그램은 갱신하지 않는다.

## 2026-07-19 — D-012

**결정:** 에이전트 오케스트레이션의 저장소 기준을 `docs/agent-orchestration/`로 두고, Lead Agent가 파일 소유권·합격 기준·QA 게이트를 작업 패킷으로 확정한다.

**이유:** 현재 실행 환경에서는 `.agents/`가 읽기 전용이고, 여러 에이전트가 공유 통합 파일을 동시에 수정하면 물리·입력·렌더링 경계와 검증 책임이 흐려진다. 저장소 문서로 역할과 작업 큐를 관리하면 Codex Desktop 병렬 스레드에서도 동일한 계약을 재사용할 수 있다.

**검증:** `PROJECT_STATUS.md`, `TASKS.md`, 역할별 파일, `AGENTS.md`의 병렬 실행 규칙을 서로 연결하고, 기능 변경은 구현 에이전트와 읽기 전용 QA 에이전트를 분리한다. 이 문서 추가는 코드 모듈 경계를 변경하지 않으므로 Archify 다이어그램은 갱신하지 않는다.

## 2026-07-19 — D-011

**결정:** 공력은 순수 `AeroModel`에서 속도 제곱 기반 전후 다운포스·드래그를 계산하고, Rapier 고정 스텝이 전·후 차축과 차체 중심에 힘으로 적용한다.

**이유:** 공력 계수와 노면 드래그 배율을 React나 Rapier API와 분리해야 단위 테스트와 자동 검증에서 같은 계산을 재사용할 수 있다. 전후 적용점은 서스펜션 하중과 차체 자세 응답을 실제 강체에 전달한다.

**검증:** 정지 시 공력 0, 속도 2배 시 공력 4배, Rapier 텔레메트리 양수, 결정적 직선·코스트다운·유한 상태 검증을 사용한다.

## 2026-07-19 — D-010

**결정:** 구동계는 순수 `Drivetrain` 명령으로 분리하고, Rapier에는 후륜 구동 토크·엔진 브레이크 토크·휠 각속도 피드백만 전달한다.

**이유:** 기어비·토크 커브·RPM·클러치·엔진 브레이크를 고정 120Hz에서 결정적으로 계산하면서, 타이어 힘과 휠 회전 적분은 Rapier 접지 경계에서 한 번만 수행해야 한다.

**검증:** 1단/7단 토크·RPM 차이, 연결 주행 중 엔진 브레이크, 정지 상태 엔진 브레이크 0, 후륜 휠 각속도와 차량 속도 통합 테스트를 사용한다.

## 2026-07-19 — D-009

**결정:** Rapier 차체 collider는 설정된 차량 질량을 collider 체적의 양의 밀도로 반영하고, 0 밀도 collider로 인해 회전 관성이 0이 되는 구성을 사용하지 않는다.

**이유:** 기존 구성은 차체 질량은 780kg으로 표시되지만 주 관성 모멘트가 `(0, 0, 0)`으로 계산되어 접지점 횡력이 있어도 yaw가 발생하지 않았다. collider 밀도로 질량과 형상 기반 관성을 함께 생성하면 현재 차체 질량을 유지하면서 회전 응답을 검증할 수 있다.

**검증:** 정지 상태 조향은 회전하지 않고, 주행 중 우회전 조향과 후륜 구동을 적용하면 Rapier quaternion과 Y축 angular velocity가 우회전 방향으로 변하는 통합 테스트를 사용한다.

## 2026-07-16 — D-008

**결정:** Milestone 1C부터 휠별 슬립 비율·슬립 각·하중 민감도·결합 그립을 순수 `TireModel`로 계산하고, Rapier 차체의 실제 Raycast 접지점에 종·횡방향 타이어 힘을 적용한다.

**이유:** 타이어 한계와 가속·제동·조향의 반응이 동일한 차체 적분기에서 계산되어야 접지점 힘과 yaw가 일관되게 검증된다. `VehicleSimulation`은 기존 입력·기어·RPM 경계를 보존하되, 매 고정 스텝 뒤 Rapier 결과로 평면 스냅샷을 동기화한다.

**포함:** 휠 회전 속도, 후륜 구동 토크의 초기 변환, 앞 58%·뒤 42% 제동 배분, 저속 분모 보호, 하중 민감도, 결합 그립, 슬립·그립 HUD와 Rapier 가속·제동 통합 테스트.

**제외:** 실차 Magic Formula 계수, 실제 타이어 열·마모·압력, 디퍼렌셜, 엔진 브레이크와 완전한 구동계 토크 모델, 경사·연석·충돌.

## 2026-07-16 — D-007

**결정:** Milestone 1B에서는 휠 운동학을 순수 TypeScript 모듈로 분리하고, 각 휠의 장착점·접지점·조향 방향·접지점 속도를 Rapier 차체 상태에서 계산한다.

**이유:** 타이어 힘 모델이 Rapier에 적용되기 전에 좌표계·조향 부호·yaw-rate 항을 독립적으로 검증해야 한다. 이 모듈은 React와 Rapier API에 의존하지 않아 AI와 자동 물리 검증에서도 재사용할 수 있다.

**포함:** 전륜 조향, 4개 휠 장착점, 차체 선속도와 각속도에 따른 접지점 속도, 차체 yaw·속도의 Rapier 동기화, 조향 HUD.

**제외:** 휠 회전 관성, 종·횡방향 타이어 힘, 슬립 비율·슬립 각 기반 그립, 실제 연석 충돌.

## 2026-07-16 — D-006

**결정:** Milestone 1A에서는 Rapier 동적 차체와 네 개의 하향 레이캐스트 서스펜션을 도입하되, X/Z 구동력과 yaw는 기존 순수 TypeScript 평면 차량 물리가 계속 소유한다.

**이유:** 현재 검증된 조작감·입력 경계를 보존하면서 실제 Raycast 접지, 스프링·댐퍼, 차체 높이를 독립적으로 검증하기 위해서다. Rapier와 평면 적분기가 동시에 종·횡방향 운동을 계산하면 두 물리 계층이 충돌한다.

**포함:** Rapier 정적 지면, 동적 차체, 4개 접지 레이, 120Hz 외력 초기화, 정적 차체 안정성 테스트, 접지·차체 높이 HUD.

**제외:** 종·횡방향 타이어 힘의 Rapier 적용, 실제 트랙 collider, 연석·경사, 충돌 손상, 롤·피치 기반 차체 거동.

## 2026-07-16 — D-005

**결정:** Rapier 연결 전 순수 TypeScript 평면 차량 물리로 `Physics Prototype v0.1`을 구현한다.

**이유:** 타이어·입력·고정 스텝의 주행 감각을 브라우저에서 빠르게 검증하고, 물리 계층을 React/R3F에서 독립시키기 위해서다.

**포함:** 엔진 구동력, 브레이크, 슬립각 기반 횡력, 결합 그립, 다운포스, 항력, 아스팔트·잔디 표면.

**제외:** 실제 타이어 열·마모, AI, 퀄리파잉, 레이스. Rapier 4휠 접지는 D-006에서 별도 도입한다.

## 2026-07-16 — D-001

**결정:** Milestone 0은 차량 물리 구현이 아니라 실행·검증 기반으로 제한한다.

**이유:** 수치가 검증되지 않은 상태에서 차량·AI·트랙을 동시에 만들면 잘못된 물리값과 구조가 누적된다.

## 2026-07-16 — D-002

**결정:** WebGL2를 초기 렌더링 기준으로 사용하고 WebGPU 전환은 후속 조사로 둔다.

**이유:** 브라우저 호환성을 우선하고 실제 병목을 측정한 뒤 렌더러 선택을 재평가한다.

## 2026-07-16 — D-003

**결정:** React UI와 물리 코드를 분리하고 `VehicleControlInput`을 입력 경계로 사용한다.

**이유:** 플레이어·AI·게임패드·휠 입력을 동일한 물리 API로 연결하고 결정성을 유지하기 위해서다.

## 2026-07-16 — D-004

**결정:** 프로젝트 전용 검증 스킬은 `.agents` 권한이 확보된 뒤 추가한다.

**이유:** 현재 작업 폴더의 `.agents`는 읽기 전용이며, 자동 검색되지 않는 임의의 위치에 스킬을 만들지 않는다.
